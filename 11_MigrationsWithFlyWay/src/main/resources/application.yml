# ===================================================================
# SECURITY UPDATE: Credentials externalized to Environment Variables
# Prevented hardcoded sensitive data vulnerability.
# ===================================================================
spring:
  application:
    name: rest-with-spring-boot-and-java-erudio
  datasource:
    drive-class-name: com.mysql.cj.jdbc.Driver
    # If the USER_DB variable does not exist in Windows, Spring falls back to 'root' as default
    url: ${URL_DB}
    username: ${USER_DB}
    password: ${PASSWORD_DB}
  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
    show-sql: false
    open-in-view: false
  logging:
    level:
      root: WARN
      com.example.erudio: DEBUG